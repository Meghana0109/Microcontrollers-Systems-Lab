Goal is to display name and real time clock on LCD display and to be able to change between the two modes
As long as the program runs, the clock runs in the background, ie, it keeps updating even if the mode changes
